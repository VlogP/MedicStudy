using System;

namespace DataTransferObject
{
    public class DataObject
    {
        public enum Role {Researcher=1,Sponsor=2,Manager=3};
        public enum DrugType { A = 0, B = 1, C = 2 };
    }
}
